// The php postinst never touches the panel's own FPM master (hestia-php.service);
// after an unattended PHP update it would keep serving the deleted old image (#191).
DPkg::Post-Invoke { "/usr/local/hestia/sbin/hestia-php-refresh || true"; };
