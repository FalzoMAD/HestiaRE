# HestiaRE — Bayes statistics in Redis. Autolearn only on clear verdicts
# (rejected spam / clearly-ham), so the classifier trains itself conservatively
# without manual rspamc learn_spam/learn_ham.
backend = "redis";
new_schema = true;
expire = 8640000; # 100 days
autolearn = true;
