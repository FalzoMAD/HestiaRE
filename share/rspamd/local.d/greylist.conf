# HestiaRE — greylisting off: delays legitimate first-contact mail and adds
# little on top of RBL + Bayes at our scale. Exim stays in control of
# accept/reject via SPAM_SCORE / SPAM_REJECT_SCORE.
enabled = false;
