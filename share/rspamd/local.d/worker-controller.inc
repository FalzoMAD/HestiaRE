# HestiaRE — controller (stats / web UI), localhost only.
# enable_password (hash) is appended at install time by h-install-hestia;
# reset any time with: rspamadm pw
bind_socket = "127.0.0.1:11334";
