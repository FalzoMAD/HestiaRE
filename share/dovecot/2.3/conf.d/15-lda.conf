########################################
# Dovecot conf file modified by Hestia #
########################################

# Base variant without sieve — h-add-sys-sieve overlays this file with the
# sieve-enabled one from share/dovecot/2.3/sieve/, h-remove-sys-sieve
# restores this one (15-lda.conf belongs to dovecot-core, so purging the
# sieve packages does not bring it back).

protocol lda {
}
