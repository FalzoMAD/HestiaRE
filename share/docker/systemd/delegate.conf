# HestiaRE: cgroup v2 delegation for rootless Docker (#389).
# Without these controllers delegated to the user manager, a rootless daemon cannot
# apply --memory/--cpus and `docker stats` stays empty. Also the hook for per-user
# resource caps via systemctl set-property user-<uid>.slice.
[Service]
Delegate=cpu cpuset io memory pids
