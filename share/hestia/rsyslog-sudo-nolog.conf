# sudo logs every allowed command line - panel passwords passed as arguments included - to
# authpriv. Where sudo accepts no logging option (sudo-rs) this drop rule is the only file-level
# cure; deployed everywhere, harmless as a second layer.
if $programname == "sudo" and $msg contains "COMMAND=/usr/local/hestia/bin/" then stop
